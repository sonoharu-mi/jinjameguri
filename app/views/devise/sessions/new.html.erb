

<div class="row no-gutters">
  <div class="col-md-5 col-sm-10 mx-2 mx-sm-auto text-center shadow-lg rounded px-0 my-5">
    <h2  class="text-dark bg-light py-4 mb-0">ログイン</h2>
    <div class="form-body p-4">
      <p>既に登録されている方はユーザー名と<br>パスワードを入力し、門をくぐりましょう。</p>
      <%= form_with model: @user, url: user_session_path do |f| %>
        <div class="input-group mb-3">
          <div class="input-group-prepend">
            <span class="input-group-text" aria-hidden="true"><i class="fas fa-user"></i></span>
          </div>
          
          <%= f.text_field :name, autofocus: true, class: "form-control", placeholder: "ユーザー名", autocomplete: "name" %>
        </div>
        <div class="input-group mb-3">
          <div class="input-group-prepend">
            <span class="input-group-text" aria-hidden="true"><i class="fas fa-lock"></i></span>
          </div>
          <%= f.password_field :password, autocomplete: "current-password", class:"form-control", placeholder: "パスワード" %>
        </div>

        <div class="actions">
          <%= f.submit "門をくぐる", class: "btn btn-block btn-outline-info mt-5" %>
        </div>
      <% end %>

      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>

