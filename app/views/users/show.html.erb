<div>
  <h1> <%= @user.name %> さんの参拝記</h1>
  <%= image_tag @user.get_profile_image(100,100) %>
</div>

<table class="table">
  <%= render @posts %>
</table>

<%= link_to "一覧に戻る", posts_path, class: "text-secondary link-underline-opacity-100-hover" %>